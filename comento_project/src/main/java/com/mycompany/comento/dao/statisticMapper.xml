<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mycompany.comento.dao.StatisticMapper">

	<select id="selectYearLogin" parameterType="string"
		resultType="hashMap">
		select count(*) as totCnt
		from statistic.requestinfo ri
		where left(ri.createDate, 2) = #{year};
	</select>

	<!-- 해당연도 총 접속자 수 -->
	<select id="selectYearTotLogin" parameterType="string"
		resultType="int">
		select count(*) as totCnt from statistic.requestinfo ri
		where left(ri.createDate, 2) = #{year};
	</select>

	<!-- 해당 월 총 접속자 수 -->
	<select id="selectMonthTotLogin" parameterType="string"
		resultType="int">
		select count(*) as totCnt from statistic.requestinfo ri
		where left(ri.createDate, 4) = #{yearMonth};
	</select>

	<!-- 월별 접속자 수 -->
	<select id="selectMonthLogin" parameterType="string" resultType="com.mycompany.comento.dto.StatisticDto$MonthDto">
		select mid(ri.createDate, 3, 2) as month,
		count(*) as
		loginNum
		from statistic.requestinfo ri
		where left(ri.createDate, 2) =
		#{year} group by mid(ri.createDate, 3, 2);
	</select>

	<!-- **************일자별 접속자수 ****************** -->

	<select id="selectDistinctMonth" parameterType="string" resultType="com.mycompany.comento.dto.StatisticDto$MonthDto">
		select distinct mid(ri.createDate, 3, 2) as month
		from statistic.requestinfo ri
		where left(ri.createDate, 2) = #{year} order
		by month asc;
	</select>

	<select id="selectDayLoginByMonth" parameterType="string" resultType="com.mycompany.comento.dto.StatisticDto$DayDto">
		select mid(ri.createDate, 5, 2) as day, count(*) as loginNum
		from statistic.requestinfo ri
		where mid(ri.createDate, 3, 2) = #{month}
		group by mid(ri.createDate, 5, 2);
	</select>


	<!-- **************************************************** -->

	<!-- 하루 평균 로그인수 -->
	<select id="selectAvgDayLogin" parameterType="string" resultType="hashMap">
		select count(*)/365 as loginNum
		from statistic.requestinfo ri
		where left(ri.createDate, 2) = #{year}
	</select>

	<!-- 휴일을 제외한 해당연도 로그인 수 -->
	<!-- 현재는 휴일을 포함 -->
	<select id="selectExceptHolidayLogin" parameterType="string"
		resultType="hashMap">
		select count(*) as totCnt
		from statistic.requestinfo ri
		where left(ri.createDate, 2) = #{year}
	</select>

	<!-- 해당연도 부서별, 월별 로그인 수 -->
	<select id="selectDeptMonthLogin" parameterType="string"
		resultType="hashMap">
		select requestCode as dept, mid(ri.createDate, 3, 2) as
		month, count(*) as loginNum
		from statistic.requestinfo ri
		where
		left(ri.createDate, 4) = #{yearMonth}
		group by requestCode,
		mid(ri.createDate, 3, 2);
	</select>

</mapper>